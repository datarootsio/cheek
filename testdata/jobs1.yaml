jobs:
  foo:
    command: date
    cron: "* * * * *"
    triggers:
      - bar
  bar:
    command:
      - /bin/bash
      - -c
      - "echo bar_foo"
  shuttle:
    command:
      - ls
      - .
    cron: "* * * * *"
  coffee:
    command: this fails
    cron: "* * * * *"
    retries: 3
