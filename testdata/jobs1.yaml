jobs:
  foo:
    command: date
    cron: "* * * * *"
    triggers:
      - bar
  bar:
    command:
      - /bin/bash
      - -c
      - "sleep 2; echo bar"
  # coffee:
  #   command: this fails
  #   cron: "* * * * *"
