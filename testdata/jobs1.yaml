jobs:
  foo:
    command: date
    cron: "* * * * *"
    triggers:
      - bar
  bar:
    command:
      - /bin/bash
      - -c
      - "sleep 2; echo bar"
  shuttle:
    command:
      - ls
      - .
    cron: "* * * * *"
  coffee:
    command: this fails
    cron: "* * * * *"
    retries: 3
